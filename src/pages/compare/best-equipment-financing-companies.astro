---
import { getEntry } from 'astro:content';
import ArticlePage from '../../layouts/ArticlePage.astro';
const entry = await getEntry('pages', 'compare/best-equipment-financing-companies');
---
<ArticlePage entry={entry} breadcrumbs={[{ name: 'Compare', url: '/' }]} />
